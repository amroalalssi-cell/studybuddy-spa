# StudyBuddy

StudyBuddy هو تطبيق ويب صغير (SPA) يساعد الطالب على تنظيم مهامه، متابعة عاداته، والحصول على مصادر تعليمية مفيدة.

---

## 🧩 المميزات

### ✅ Tasks (المهام)
- إضافة مهمة مع عنوان، وصف، تاريخ الاستحقاق، أولوية، وفئة.
- تعديل / حذف المهام.
- تمييز المهمة كمكتملة.
- تصفية حسب الحالة (All / Active / Completed).
- تصفية حسب الفئة.
- ترتيب حسب التاريخ أو الأولوية.

### 📈 Dashboard
- عرض عدد المهام التي ستنتهي قريبًا.
- عرض عدد المهام المكتملة.
- عرض تقدم الإنجاز بشكل شريط.
- عرض مهام اليوم فقط.
- عرض streak العادات (عدد الأيام المتتالية المكتملة خلال الأسبوع).

### 🧠 Habits (العادات)
- إضافة عادة مع هدف (1-7 أيام).
- متابعة تقدم كل يوم من أيام الأسبوع.
- إعادة ضبط الأسبوع تلقائيًا عندما يبدأ أسبوع جديد.

### 📚 Resources (المصادر)
- عرض مصادر تعليمية من ملف `resources.json`.
- البحث داخل المصادر.
- تصفية حسب الفئة.
- إضافة/إزالة المفضلة.

### ⚙️ Settings
- تبديل الوضع الليلي (Dark Mode).
- إعادة ضبط جميع البيانات (Reset).

---

## 🗂️ الملفات

| الملف | الوصف |
|-------|-------|
| `index.html` | واجهة المستخدم + تنقل بين الصفحات |
| `style.css` | تصميم التطبيق |
| `js/state.js` | تعريف State (البيانات) |
| `js/storage.js` | حفظ/تحميل/إعادة ضبط البيانات من LocalStorage |
| `js/ui.js` | التنقل بين Views |
| `js/app.js` | المنطق الكامل للتطبيق |
| `resources.json` | بيانات المصادر |

---

## 🚀 طريقة التشغيل

1. افتح المشروع في مجلد واحد.
2. شغل الملف `index.html` في المتصفح.

> **ملاحظة:** إذا ظهرت مشكلة في تحميل `resources.json` عند تشغيل المشروع من ملف محلي، استخدم Live Server أو خادم محلي مثل:
```bash
npx http-server
